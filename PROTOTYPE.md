# 2026 GSE — 첫 렌더링 프로토타입

2026-09-11 / C++ · OpenGL 3.3 Core · FreeGLUT · GLEW

기획서의 어두운 현대 도시, 공간 왜곡, 동행 요괴의 무기 변신을 확인하는
조작 가능한 렌더링 데모입니다. 현재 화면은 코드로 만든 도형과 글꼴로 구성됩니다.
화면 제목 “The Nameless District”와 요괴 외형은 임시 디자인입니다.

## 실행

1. 이 폴더의 `SimpleGame.sln`을 Visual Studio 2022에서 엽니다.
2. **x64 / Debug** 또는 **x64 / Release**를 선택하고 빌드합니다.
3. F5 또는 출력 폴더의 `SimpleGame.exe`를 실행합니다.

필요 구성 요소: MSVC v143 C++ 도구, Windows SDK, OpenGL 3.3을 지원하는 드라이버.
실행용 GLEW/FreeGLUT DLL은 빌드 시 출력 폴더에 복사됩니다.
프로토타입 셰이더와 글꼴 아틀라스는 실행 시 준비하므로 작업 폴더에 의존하지 않습니다.
기본 창은 1280×800이며 다른 크기에서는 장면과 HUD를 함께 조절합니다.
동료 대사와 목표 안내는 한글로 표시합니다. Windows 맑은 고딕에서 ASCII와
현대 한글 11,172자를 아틀라스로 만들고 UTF-8 문자열을 출력합니다.
프로토타입 제목·개발 상태·키 안내와 건물 간판은 영문을 유지합니다.

## 조작

| 입력 | 동작 |
| --- | --- |
| WASD | 화면 방향 기준 이동 |
| Q | 동행 요괴 ↔ 검 형태 변신 |
| Space | 검 형태에서 공격 궤적 표시 |
| E | 시작 구역의 붉은 균열 근처에서 기억 회수 |
| V | 건물·균열의 왜곡 애니메이션 켜기/끄기 |
| G | 청크 경계, 좌표, 상주 청크 수 표시 |
| R | 시작 위치로 돌아가기 |
| H | 전체 후처리 켜기/끄기 (같은 장면 비교) |
| B | HDR 블룸(빛 번짐) 켜기/끄기 |
| N | 비네트 켜기/끄기 |
| M | 가장자리 흐림 켜기/끄기 |
| [ / ] | 노출 감소/증가 (25~300%) |
| 0 | 후처리 설정 기본값으로 복원 |
| Esc | 종료 |

균열에 다가가 E를 누르면 목표, 균열 색과 동료 대사가 바뀝니다.
이 대화는 로컬에 작성된 고정 대사이며 AI 서버를 호출하지 않습니다.
공격은 시각 효과 검증용이며 아직 적·피해·패링 판정은 없습니다.

## 구현 범위

- 등각 투영 2.5D 도시: 도로, 횡단보도, 건물, 창문, 옥상 장치, 간판, 가로등.
- 어두운 청록색 분위기, 비, 혈흔, 붉은 균열과 떠다니는 입자.
- 플레이어 이동, 건물 충돌, 지수 보간 카메라 추적.
- 앞 건물이 플레이어를 가릴 때 반투명 처리.
- 시드 2026 기반 청크 생성, 주변 5×5 청크 유지와 재생성.
- 64비트 청크 좌표와 청크 내부 좌표 분리. 렌더링은 상대 좌표만 사용.
- 같은 청크 재방문 시 동일한 건물 높이·배치 특성 복원.
- 동행 요괴 애니메이션, 무기 변신 효과, 공격 궤적.
- 거리 조건을 가진 기억 회수 상호작용과 목표 UI.
- GPU 자원 정리, 창 크기 변경, 실행 파일 기준 캡처 저장.
- HDR 장면 버퍼, 블룸, 톤매핑, 비네트와 가장자리 흐림. 후처리 뒤 한글 UI 합성.

건물 위치는 동일한 블록 규칙이며 높이·창문·간판 등만 시드에 따라 달라집니다.
현재 생성은 가벼운 동기 처리입니다. 비동기 스트리밍, 지형 다양화, 저장,
요괴 성장과 영구 융합, AI 대화·히든 퀘스트 생성은 아직 구현하지 않았습니다.
메모리 회수 상태도 실행 중에만 유지됩니다.
V는 왜곡된 건물과 균열의 움직임을 전환하며 비·요괴 애니메이션은 유지됩니다.

## 코드 위치

- `SimpleGame.cpp`: 진입점, OpenGL 초기화, 입력과 타이머, 자동 렌더링 검증.
- `PrototypeRenderer.h/.cpp`: VAO/VBO 기반 배치 렌더링, 도형, 글꼴 아틀라스, BMP 캡처.
- `PrototypeWorld.h/.cpp`: 청크, 좌표, 이동과 충돌, 장면, 요괴, 상호작용, HUD.
- `PostProcessor.h/.cpp`: HDR FBO, 가우시안 필터, 하이라이트 추출, 톤매핑과 최종 합성.
- `Dependencies/Runtime`: 기존 프로젝트가 사용하던 실행용 DLL.
- 기존 `Renderer.cpp/.h`, `Shaders`: 원본 예제 참고용. 새 진입점에서는 사용하지 않습니다.

## 검증 명령

프로젝트 루트에서:

```powershell
.\x64\Debug\SimpleGame.exe --self-test
.\x64\Debug\SimpleGame.exe --smoke-test
```

`--self-test`는 GL 창 없이 음수 좌표 정규화, 먼 좌표의 결정적 생성,
청크 상주 개수, 재방문 복원, 충돌, 이동, 변신 키 반복 방지, 공격과
상호작용 거리 조건을 검사합니다. 실패하면 종료 코드 2를 반환합니다.

`--smoke-test`는 위 검사 후 실제 OpenGL 창에서 동행, 무기/공격,
기억 회수 후 대사, 후처리 OFF 비교, 960×600 및 961×601 창 크기 변경,
1280×800 복원 화면을 렌더링해 실행 파일 옆에 BMP로 저장하고 종료합니다.
실제 HDR 버퍼의 1.0 초과 밝기, 효과별 이미지 변화, UI 분리와 전체 OFF 동작도 검사합니다.
각 캡처의 저장 결과와 OpenGL 오류를 검사합니다. 스모크 테스트는 자동 입력으로
상태를 전환하며, 사람이 직접 수행한 플레이 테스트를 의미하지는 않습니다.

## 확인 결과

2026-09-11 확인: x64 Debug/Release 빌드 성공, 각 구성에서 논리 검사 10개,
후처리 이미지 검사 6개, HDR 밝기 검사와 7가지 framebuffer 캡처 통과.
HDR 장면의 최대 RGB 값 약 4.82, OpenGL 오류 0.
AMD Radeon(TM) Graphics에서 실행했습니다.
이미지는 `docs/prototype/companion.png`, `weapon.png`, `memory.png`, `resized.png` 및
`post-off.png`, `resized-odd.png`, `restored.png`에 있습니다.

## HDR 후처리 구성

렌더링 순서:

1. 장면 색을 sRGB에서 선형 공간으로 변환해 RGBA16F FBO에 렌더링합니다.
2. 요괴·검·균열·간판·가로등은 별도 energy 값으로 1.0을 넘는 발광을 표현합니다.
3. 장면의 밝은 부분을 추출해 절반 해상도의 두 버퍼에서 가우시안 블룸을 계산합니다.
4. 별도의 두 버퍼에서 전체 장면의 흐린 이미지를 준비합니다.
5. 화면 중앙은 원본, 가장자리는 흐린 이미지로 점진적으로 섞습니다.
6. 블룸을 더하고 타원형 비네트와 노출, ACES 근사 톤매핑을 적용합니다.
7. sRGB로 변환해 기본 화면에 출력하고, 그 뒤 대사와 HUD를 그립니다.

HUD는 어두워지거나 흐려지지 않습니다. 반투명 패널을 통해 보이는 배경만 후처리됩니다.
기존 사각형 테두리 음영은 제거했으며, 이제 비네트는 실제 장면 결과에 적용됩니다.
기존 원형 광무리도 부드러운 알파 보간으로 바꿔 톤매핑 후 동심원이 보이지 않게 했습니다.

기본값 (PostSettings):

- exposure = 1.10
- bloomStrength = 0.24
- vignetteStrength = 0.48
- edgeBlurStrength = 0.95
- 하이라이트 기준은 선형 밝기 1.0, 경계 부근은 완만하게 추출
- 모든 효과 ON, H로 OFF 시 선형 장면을 sRGB로만 변환하여 출력

설정은 실행 중에만 유지됩니다. 강도 기본값은 PostProcessor.h에서 조절합니다.
흐림은 화면 중심에서의 거리를 사용하는 효과이며 깊이 기반 피사계 심도는 아닙니다.
HDR은 부동소수점 내부 렌더링을 의미합니다. 출력은 일반 SDR 화면으로 톤매핑하며,
HDR10 모니터 출력이나 Windows HDR 전환 기능은 포함하지 않습니다.

렌더 타깃은 크기가 바뀔 때 재생성하고 FBO 완전성을 검사합니다.
해상도 1280×800에서 5개 HDR 색상 타깃은 약 15.6 MiB를 사용합니다.
블룸과 흐림은 각각 4회 필터 패스를 사용하며, OFF인 효과는 필터 패스를 건너뜁니다.
후처리 셰이더도 C++ 소스 안에 포함되어 별도 셰이더 파일 경로에 의존하지 않습니다.

## 다음 단계

1. 플레이어·동행 요괴의 최종 크기, 카메라 각도, 도시 밀도를 결정합니다.
2. 적 1종, 공격 히트박스, 피격과 회피를 추가합니다.
3. 임시 도형을 스프라이트 또는 모델·애니메이션 에셋으로 교체합니다.
4. 영구 융합 데모와 융합 결과의 기억·외형 계승을 구현합니다.
5. 저장 구조와 검증된 대화 이벤트를 마련한 후 AI를 연결합니다.
